# Production environment configuration for Docker deployment
# This file is used during the Docker build process
# The backend URL is empty because nginx will proxy all API requests
VITE_API_BASE_URL=
